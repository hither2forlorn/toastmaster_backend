name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render service
        run: |
          curl -s -o /dev/null -w "%{http_code}\n" https://toastmaster-backend.onrender.com/api/health
